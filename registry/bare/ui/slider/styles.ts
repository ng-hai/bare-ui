import { tv } from "@/registry/bare/lib/tv.config";

export const sliderStyles = tv({
  slots: {
    root: [""],
    label: [""],
    value: [""],
    control: [""],
    track: [""],
    thumb: [""],
    indicator: [""],
  },
});
