import { tv } from "@/registry/bare/lib/tv.config";

export const toolbarStyles = tv({
  slots: {
    root: [""],
    group: [""],
    button: [""],
    link: [""],
    input: [""],
    separator: [""],
  },
});
