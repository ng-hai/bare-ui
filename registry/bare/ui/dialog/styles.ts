import { tv } from "@/registry/bare/lib/tv.config";

export const dialogStyles = tv({
  slots: {
    root: [""],
    trigger: [""],
    portal: [""],
    backdrop: [""],
    popup: [""],
    viewport: [""],
    title: [""],
    description: [""],
    close: [""],
  },
});
