import { tv } from "@/registry/bare/lib/tv.config";

export const numberFieldStyles = tv({
  slots: {
    root: [""],
    group: [""],
    increment: [""],
    decrement: [""],
    input: [""],
    scrubArea: [""],
    scrubAreaCursor: [""],
  },
});
