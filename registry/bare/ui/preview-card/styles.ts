import { tv } from "@/registry/bare/lib/tv.config";

export const previewCardStyles = tv({
  slots: {
    root: [""],
    trigger: [""],
    portal: [""],
    backdrop: [""],
    positioner: [""],
    popup: [""],
    arrow: [""],
    viewport: [""],
  },
});
