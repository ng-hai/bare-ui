import { tv } from "@/registry/bare/lib/tv.config";

export const tooltipStyles = tv({
  slots: {
    root: [""],
    trigger: [""],
    portal: [""],
    positioner: [""],
    popup: [""],
    arrow: [""],
  },
});
