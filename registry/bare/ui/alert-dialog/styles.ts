import { tv } from "@/registry/bare/lib/tv.config";

export const alertDialogStyles = tv({
  slots: {
    trigger: [""],
    portal: [""],
    backdrop: [""],
    popup: [""],
    viewport: [""],
    title: [""],
    description: [""],
    close: [""],
  },
});
