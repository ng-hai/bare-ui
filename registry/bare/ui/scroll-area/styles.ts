import { tv } from "@/registry/bare/lib/tv.config";

export const scrollAreaStyles = tv({
  slots: {
    root: [""],
    viewport: [""],
    scrollbar: [""],
    content: [""],
    thumb: [""],
    corner: [""],
  },
});
