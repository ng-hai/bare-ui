import { tv } from "@/registry/bare/lib/tv.config";

export const fieldStyles = tv({
  slots: {
    root: [""],
    label: [""],
    error: [""],
    description: [""],
    control: [""],
    item: [""],
  },
});
