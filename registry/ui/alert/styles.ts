import { tv } from "@/registry/lib/tv-config";

export const alertStyles = tv({
  slots: {
    root: [""],
    icon: [""],
    content: [""],
    title: [""],
    description: [""],
    action: [""],
    close: [""],
  },
});
